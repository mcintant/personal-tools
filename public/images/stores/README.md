# Store logos for price directory column headers

Add one image per store; filename should match the key in `src/data/priceDirectoryMetadata.js` (e.g. `tj.png`, `wf.png`, `union.png`).

Suggested size: 32–48px height for a square or wide logo.
