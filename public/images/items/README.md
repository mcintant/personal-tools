# Product images for price directory rows

Add one image per product/item; filename should match the path in `src/data/priceDirectoryMetadata.js` (e.g. `banana.png`, `avocado.png`, `chicken.png`).

Suggested size: 40–56px for a small thumbnail.
